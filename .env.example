# Example environment variables for DevForge AI
# Copy this to `.env` or `.env.local` (do NOT commit real keys)

# OpenAI API key (required to enable general-purpose AI answers)
VITE_OPENAI_API_KEY=

# (Optional) OpenAI model to use. Defaults to gpt-3.5-turbo if not set.
VITE_OPENAI_MODEL=gpt-4
